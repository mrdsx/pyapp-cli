# PyApp CLI

PyApp CLI is a powerful tool that makes python project setup painless.

# Supported platforms

The CLI can be run on Ubuntu Linux, but it's not guaranteed the CLI works on other platforms.

# Get started

Before setting up new project, install Python and appropriate package manager you'll use for your project. PyAPP CLI supports following package managers:

- pip
- poetry
- uv
