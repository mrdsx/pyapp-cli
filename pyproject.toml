[project]
name = "pyapp-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12,<4.0"
dependencies = [
    "colorama>=0.4.6",
    "fire>=0.7.1",
    "inquirerpy>=0.3.4",
    "pydantic>=2.12.5",
]
